package practice;

/**
 * [1] Given a text, divide it into multiple lines, in such a way that every line is no longer than given number of characters
 * [2] Don't break the words. If breakpoint land on a word move the word to the next line.
 */

public class Lines {
    public String process(String text, int lineLength) {
        return text;
    }
}
